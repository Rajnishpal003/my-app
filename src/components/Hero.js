import React from 'react';

const Hero = () => {
  return (
    <section id="hero">
      <h1>Welcome to MyApp</h1>
      <p>Your all-in-one solution for managing everything!</p>
      <button>Get Started</button>
      <button>Learn More</button>
    </section>
  );
};

export default Hero;
